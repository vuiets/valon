<main class="dt-row items-baseline">

    <section class="dtc miw-75 mw6 pa3 ph5-ns">
        <p class="lh-copy measure">
            Vish is a Developer from Bangalore, India, living and working in Chennai, India.
        </p>

        <p class="lh-copy measure">
            Amongst thousands of photos, it's easy to lose track of my favorites. This little website serves as a home
            for
            the photos I adore the most
        </p>

    </section>

	{{!--{{#each countries as |country|}}--}}
	{{!--<div class="dtc mr2"><a href="#" class="f4 f2-ns b db pa2 link dim mid-gray">{{country}}</a></div>--}}
	{{!--{{/each}}--}}

	{{#each categories as |category index|}}
        <div class="dtc v-mid pr4">
            <a href="#{{index}}">
                <img class="db w-auto calc-h-100 mb2" src="https://source.unsplash.com/category/{{category}}">
            </a>
            <p class="mb0">{{category}}</p>
        </div>
	{{/each}}

</main>

